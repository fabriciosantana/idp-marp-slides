@startuml
title java.util.LinkedHashSet - principais métodos

skinparam classAttributeIconSize 0

class "LinkedHashSet<E>" as LinkedHashSet {
  + LinkedHashSet()

  + add(e: E): boolean
  + addFirst(e: E): void
  + addLast(e: E): void

  + getFirst(): E
  + getLast(): E
  + removeFirst(): E
  + removeLast(): E
  + reversed(): SequencedSet<E>

  + remove(o: Object): boolean
  + contains(o: Object): boolean
  + iterator(): Iterator<E>
  + size(): int
  + isEmpty(): boolean
  + clear(): void
}

@enduml
